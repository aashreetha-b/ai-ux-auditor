# Context-Aware AI UX Auditor — One-File Showcase

This is a single-file, demo-ready showcase of an AI UX Auditor:
- heuristic + ViT embedding + fusion regressor
- attention-rollout heatmaps
- deterministic LLM-stub for recommendations
- AI redesign mockup (structural edits)
- PDF audit report generator
- Streamlit UI + PDF download

## Quickstart (fast; <1 hour)

1. Create venv & install:
   ```bash
   python -m venv venv
   source venv/bin/activate       # Windows: venv\Scripts\activate
   pip install -r requirements.txt
